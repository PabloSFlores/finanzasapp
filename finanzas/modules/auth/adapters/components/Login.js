import { StyleSheet, Text, View } from 'react-native'
import React, {useState} from 'react'

export default function Login(props) {
    const [name/*GET*/, setName/*SET*/] = useState('');
  return (
    <View>
      <Text>Login</Text>
    </View>
  )
}

const styles = StyleSheet.create({})